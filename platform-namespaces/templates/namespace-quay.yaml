{{- if .Values.namespaces.quay.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: quay
  labels:
    app.kubernetes.io/name: quay
    app.kubernetes.io/part-of: etx-lab-infrastructure
    demo.redhat.com/platform: "true"
  annotations:
    description: "Red Hat Quay - Container registry"
    argocd.argoproj.io/sync-wave: "0"
{{- end }}
