---
- name: Bootstrap ETX platform GitOps entrypoints
  hosts: localhost
  connection: local
  gather_facts: false

  roles:
  - role: etx_bootstrap_preflight
  - role: etx_gitops_bootstrap
  - role: etx_postflight_validation
